import { FilterIdentifPipe } from './filter-identif.pipe';

describe('FilterIdentifPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIdentifPipe();
    expect(pipe).toBeTruthy();
  });
});
