import { GetFaitsBySecteurPipe } from './get-faits-by-secteur.pipe';

describe('GetFaitsBySecteurPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFaitsBySecteurPipe();
    expect(pipe).toBeTruthy();
  });
});
