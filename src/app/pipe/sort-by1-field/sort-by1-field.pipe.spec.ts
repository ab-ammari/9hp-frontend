import { SortBy1FieldPipe } from './sort-by1-field.pipe';

describe('SortBy1FieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBy1FieldPipe();
    expect(pipe).toBeTruthy();
  });
});
