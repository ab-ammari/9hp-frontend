import { FilterByIdEnsPipe } from './filter-by-id-ens.pipe';

describe('FilterByIdEnsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIdEnsPipe();
    expect(pipe).toBeTruthy();
  });
});
