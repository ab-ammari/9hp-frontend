import { FilterByUniteIdPipe } from './filter-by-unite-id.pipe';

describe('FilterByUniteIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByUniteIdPipe();
    expect(pipe).toBeTruthy();
  });
});
