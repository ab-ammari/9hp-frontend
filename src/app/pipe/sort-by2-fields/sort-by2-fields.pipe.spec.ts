import { SortBy2FieldsPipe } from './sort-by2-fields.pipe';

describe('SortBy2FieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBy2FieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
