import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.castor.app',
  appName: 'castor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
